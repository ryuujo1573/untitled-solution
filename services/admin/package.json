{
  "name": "@cnmjs/admin",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:ui": "bun --filter @cnmjs/admin-front dev",
    "build": "vite build && wrangler deploy --minify",
    "build:ui": "bun --filter @cnmjs/admin-front build",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "typescript": "^5.9.3"
  }
}
